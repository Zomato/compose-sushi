package com.zomato.sushi.compose.atoms.icon

/**
 * Provides default values for SushiIcon components.
 * These defaults are used when specific properties are not provided in SushiIconProps.
 *
 * @author gupta.anirudh@zomato.com
 */
object SushiIconDefaults {
    /**
     * Default size for icons when no explicit size is specified.
     * Uses SushiIconSize.Size100 as the standard size.
     */
    public val size = SushiIconSize.Size100
}